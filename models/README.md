# ONNX Models

Place the following ONNX model files in this directory:

- `hubert_base.onnx` - HuBERT model for feature extraction
- `rmvpe.onnx` - RMVPE model for F0 pitch extraction
- `Trump_RVC_v2.onnx` - RVC Trump voice conversion model

These models are required for the vocoder to function.
